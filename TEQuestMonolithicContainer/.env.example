# Django
DJANGO_ALLOWED_HOSTS=localhost,127.0.0.1,0.0.0.0,.kavia.ai
DJANGO_DEBUG=true

# Server binding (used by deployment; run with: python manage.py runserver 0.0.0.0:3000)
PORT=3000
HOST=0.0.0.0

# Email settings
EMAIL_HOST=smtp.example.com
EMAIL_PORT=587
EMAIL_USE_TLS=true
EMAIL_HOST_USER=
EMAIL_HOST_PASSWORD=
DEFAULT_FROM_EMAIL=noreply@example.com

# Password reset and site URL (point to your frontend)
SITE_URL=http://localhost:3000
PASSWORD_RESET_PATH=/reset-password
